{
  "rules": {
    "items": {
      ".read": "auth.uid != null && query.orderByChild == 'messageUid' && query.equalTo == auth.uid",
      ".write": "auth != null",
      ".indexOn": "messageUid"
    }
  }
}
